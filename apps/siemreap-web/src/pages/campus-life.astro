---
import { existsSync } from 'node:fs';
import SrLayout from '../layouts/SrLayout.astro';
import Footer from '../components/sr/Footer.astro';
import PageHead from '../components/sr/PageHead.astro';
import Cta from '../components/sr/Cta.astro';
import PhotoSlot from '../components/sr/PhotoSlot.astro';
import CampusDive from '../components/sr/CampusDive.astro';

// The campus photo goes at public/images/campus/campus.jpg; until it lands
// the hero falls back to the GonPreah community photo.
const CAMPUS_PHOTO = existsSync('public/images/campus/campus.jpg') ? '/images/campus/campus.jpg' : '/images/gonpreah-community.jpg';

const STATS: { n: number; suffix: string; label: string }[] = [
  { n: 40, suffix: '', label: 'full-time staff' },
  { n: 4, suffix: '', label: 'schools on campus' },
  { n: 1, suffix: '', label: 'campus, one family' },
  { n: 20, suffix: ' min', label: 'to Angkor Wat' },
];

// Living on the base. Draft copy for the team to correct — rhythms, rooms
// and rules are placeholders until staff confirm them.
const COMMUNITY: { km: string; title: string; body: string; photo?: string; alt?: string; label: string; pos?: string }[] = [
  { km: 'គ្រួសារ', title: 'Khmer and international, together', body: 'Forty staff and a changing family of students, volunteers and teams share this campus. Khmer is the first language and English is everywhere. You will learn a few words in your first week and be laughed at kindly for the rest.', photo: '/images/campus/friends-temple.jpg', alt: 'Two friends, one international and one Khmer, sitting and talking among the stones of a temple', label: '', pos: '50% 35%' },
  { km: 'ថ្វាយបង្គំ', title: 'We worship together', body: 'Worship and prayer are part of the rhythm of the week — as a whole community, in schools, and in small groups. Some of it in Khmer, some in English, most of it both.', photo: '/images/gonpreah-worship-1.jpg', alt: 'Students worshipping together on the Siem Reap campus', label: '' },
  { km: 'បម្រើ', title: 'We work together', body: 'Everyone carries part of the load: cleaning, cooking, setting up, driving, building. Serving the community starts with serving each other.', photo: '/images/campus/work-bamboo.jpg', alt: 'A young man grinning as he carries bamboo poles over his shoulder along a campus path', label: '', pos: '50% 30%' },
];

// The base week. Every weekday shares the same frame; the 8:30 slot changes.
type Slot = { time: string; title: string; note?: string; hi?: boolean };
const WEEK: { day: string; full: string; slots: Slot[] }[] = [
  { day: 'Mon', full: 'Monday', slots: [
    { time: '7:30 – 8:00', title: 'Breakfast' }, { time: '8:00', title: 'Morning chores' },
    { time: '8:30 – 9:30', title: 'Base worship', note: 'The whole base, one room, in Khmer and English.', hi: true },
    { time: '12:30 – 1:30', title: 'Lunch' }, { time: '6:30 – 7:30', title: 'Dinner' },
  ] },
  { day: 'Tue', full: 'Tuesday', slots: [
    { time: '7:30 – 8:00', title: 'Breakfast' }, { time: '8:00', title: 'Morning chores' },
    { time: '8:30 – 9:30', title: 'Staff meeting', note: 'Where the week gets planned and prayed over.', hi: true },
    { time: '12:30 – 1:30', title: 'Lunch' }, { time: '6:30 – 7:30', title: 'Dinner' },
  ] },
  { day: 'Wed', full: 'Wednesday', slots: [
    { time: '7:30 – 8:00', title: 'Breakfast' }, { time: '8:00', title: 'Morning chores' },
    { time: '8:30 – 9:30', title: 'Intercession', note: 'Praying for Cambodia, the nations, and each other.', hi: true },
    { time: '12:30 – 1:30', title: 'Lunch' }, { time: '6:30 – 7:30', title: 'Dinner' },
  ] },
  { day: 'Thu', full: 'Thursday', slots: [
    { time: '7:30 – 8:00', title: 'Breakfast' }, { time: '8:00', title: 'Morning chores' },
    { time: '8:30 – 9:30', title: 'Small groups', note: 'A few people, honest conversation, prayer.', hi: true },
    { time: '12:30 – 1:30', title: 'Lunch' },
    { time: '6:15 – 7:30', title: 'Community night', note: 'Everyone together — worship, stories, food after.', hi: true },
    { time: '7:30 – 8:30', title: 'Dinner', note: 'Later on Thursdays, after community night.' },
  ] },
  { day: 'Fri', full: 'Friday', slots: [
    { time: '7:30 – 8:00', title: 'Breakfast' }, { time: '8:00', title: 'Morning chores' },
    { time: '8:30 – 9:30', title: 'Staff development', note: 'Training and growth for the staff team.', hi: true },
    { time: '12:30 – 1:30', title: 'Lunch' }, { time: '6:30 – 7:30', title: 'Dinner' },
  ] },
  { day: 'Sat', full: 'Saturday', slots: [
    { time: 'All day', title: 'Sabbath', note: 'A day of rest for the whole base. No chores, no meetings — sleep, the city, a slow meal.', hi: true },
  ] },
  { day: 'Sun', full: 'Sunday', slots: [
    { time: 'Morning', title: 'Church', note: 'Everyone joins a local congregation — Khmer and international churches around Siem Reap.', hi: true },
  ] },
];

// Around campus. Draft captions — swap photos and wording as staff confirm.
const FACILITIES: { title: string; note: string; photo?: string; alt?: string; label?: string; pos?: string }[] = [
  { title: 'The campus', note: 'Four storeys of classrooms, rooms and rooftop terraces, twenty minutes from Angkor.', photo: '/images/campus/campus.jpg', alt: 'The campus from the air', pos: '50% 50%' },
  { title: 'GP Cafe', note: 'Our café on campus. Coffee, iced drinks and the conversations that carry on after class.', photo: '/images/campus/gp-cafe-sign.jpg', alt: 'The GP Cafe sign among green leaves', pos: '50% 40%' },
  { title: 'Worship hall', note: 'Where the base gathers on Monday mornings and Thursday nights.', photo: '/images/gonpreah-worship-2.jpg', alt: 'Worship on campus' },
  { title: 'Classrooms', note: 'Where DTS and DBS meet for lectures, in Khmer and English.', photo: '/images/schools/dbs-class.jpg', alt: 'Students in a classroom', pos: '50% 40%' },
  { title: 'The lounge', note: 'Bean bags, books and shade — the place between classes.', photo: '/images/schools/dbs-small-groups.jpg', alt: 'Students reading on bean bags', pos: '50% 60%' },
  { title: 'Rooms', note: 'Shared rooms for students, with fans, wifi and a bed that is yours for the school.', label: 'Drop a photo — a student room' },
];

const WHO: [string, string][] = [
  ['Students', 'DTS and DBS students live on campus for the length of their school, in shared rooms with classmates.'],
  ['Volunteers', 'Short or long term, volunteers live and eat with the community and join its rhythms while they serve.'],
  ['New staff', 'Staff joining us find their feet here first: language, community, city — before the work takes over.'],
  ['Teams', 'Visiting teams stay on or near the base, eat with us, and see what living here is actually like.'],
];

// Siem Reap for people who are coming to live here for a while, not just
// pass through. One panel per kind of day off, plus the practical one.
type Place = { name: string; note: string };
type Panel = { title: string; km: string; intro: string; places: Place[]; label: string; photo?: string; alt?: string; pos?: string };
const CITY: Panel[] = [
  {
    title: 'Temples & history', km: 'អង្គរ',
    intro: 'Siem Reap is the gateway to Angkor. The temples are twenty minutes from the base and you will want more than one visit.',
    places: [
      { name: 'Angkor Wat at sunrise', note: 'The one everyone comes for. Go early, stay for the quiet after the crowds leave.' },
      { name: 'Angkor Thom & the Bayon', note: 'The walled city and its temple of two hundred stone faces.' },
      { name: 'Ta Prohm', note: 'The temple the jungle took back — roots over stone.' },
      { name: 'Banteay Srei', note: 'Smaller, further out, and the finest carving in Angkor.' },
      { name: 'Angkor National Museum', note: 'Worth an afternoon before or after the temples themselves.' },
    ],
    label: 'Drop a photo — Angkor Wat', photo: '/images/campus/angkor-backpack.jpg', alt: 'A student with a backpack looking up at the towers of Angkor Wat', pos: '60% 50%',
  },
  {
    title: 'Food & cafés', km: 'ម្ហូប',
    intro: 'Khmer food is gentler than Thai — sour, herby, coconut-sweet. Learn three dishes and you will eat well anywhere.',
    places: [
      { name: 'Fish amok', note: 'Steamed fish curry in banana leaf. Order it your first week.' },
      { name: 'Num banh chok', note: 'Khmer noodles for breakfast, from a lady with a basket, before 9am.' },
      { name: 'Old Market (Psar Chas)', note: 'Fruit, fabric, coffee, everything. Bargain gently.' },
      { name: 'Kandal Village', note: 'Small streets of cafés and shops a short walk from the river.' },
      { name: 'Night markets & Pub Street', note: 'Loud, bright, cheap, and a good place to take a visiting team for one evening.' },
    ],
    label: 'Drop a photo — a market or café', photo: '/images/campus/fruit-table.jpg', alt: 'Trays of watermelon, grapes and longan being served at a shared meal', pos: '50% 50%',
  },
  {
    title: 'Nature & day trips', km: 'ធម្មជាតិ',
    intro: 'When you need space, the countryside starts ten minutes out of town.',
    places: [
      { name: 'Tonlé Sap floating villages', note: 'Kampong Phluk or Kampong Khleang — stilt houses and flooded forest, best in the wet season.' },
      { name: 'Phnom Kulen', note: 'Waterfalls, a reclining Buddha, and the river of a thousand lingas. A full day.' },
      { name: 'Countryside by bicycle', note: 'Rice fields, water buffalo and villages on the road out of town.' },
      { name: 'Sunset from Pre Rup', note: 'Or Phnom Bakheng — both inside the Angkor park.' },
    ],
    label: 'Drop a photo — Tonlé Sap or Kulen', photo: '/images/campus/tonle-sap-boat.jpg', alt: 'A blue two-deck tour boat full of passengers crossing the Tonlé Sap under a wide sky', pos: '50% 55%',
  },
  {
    title: 'Culture & evenings', km: 'វប្បធម៌',
    intro: 'Siem Reap has more going on after dark than any city its size in Cambodia.',
    places: [
      { name: 'Phare, the Cambodian Circus', note: 'Acrobatics and storytelling by young Khmer performers. Take everyone.' },
      { name: 'Apsara dance', note: 'Classical Khmer dance, usually with dinner.' },
      { name: 'Made in Cambodia market', note: 'Local makers, fair prices, good gifts to send home.' },
      { name: 'Local churches', note: 'Khmer and international congregations — ask staff where they worship on Sundays.' },
    ],
    label: 'Drop a photo — Phare or an evening out', photo: '/images/campus/apsara.jpg', alt: 'An Apsara dancer in gold costume performing on stage', pos: '50% 30%',
  },
];

const PRACTICAL: [string, string][] = [
  ['Getting around', 'Tuk-tuks are everywhere; use the PassApp or Grab apps for a fair fixed price. Bicycles work well in town. Traffic is calmer than Phnom Penh, but still look twice.'],
  ['Money', 'US dollars and Khmer riel are both used. Small change comes back in riel (roughly 4,000 to the dollar). ATMs are common; most give dollars.'],
  ['Phone & internet', 'Buy a local SIM on arrival — Smart, Cellcard and Metfone all work. Data is cheap and the campus has wifi.'],
  ['Weather', 'Cool and dry from November to February, hot from March to May, wet from June to October. The rain comes hard and leaves fast.'],
  ['Dress', 'Cover shoulders and knees at temples and in villages. Light and modest is right almost everywhere.'],
  ['Health', 'Drink bottled or filtered water. Pharmacies and international clinics are in town; staff will point you to the ones we use.'],
];
---
<SrLayout title="Campus Life | YWAM Siem Reap" spacer={false}
  description="What living at YWAM Siem Reap is like — Khmer and international, students, volunteers, staff and teams in one community — and what to do in Siem Reap when the day is done.">
  <PageHead eyebrow="YWAM Siem Reap" km="ជីវិតសហគមន៍" title="Campus Life" highlight="Life"
    lead="Live in a cross-cultural community centred on Jesus. Make His name known in Cambodia and beyond."
    photo={CAMPUS_PHOTO} full center photoAlt="The YWAM Siem Reap campus from the air — a white four-storey building with orange tiled roofs and rooftop terraces, surrounded by trees" />


  <CampusDive aerial="/images/campus/campus-aerial-wide.jpg" front="/images/campus/campus-front.jpg" />

  <section class="sec cl-community" style="padding-bottom:0;">
    <div class="wrap">
      <div class="reveal" style="max-width:62ch;">
        <p class="eyebrow">Living in community</p>
        <h2 class="disp" style="margin:14px 0 0;font-size:clamp(30px,3.8vw,50px);font-weight:500;line-height:1.08;">One family, two languages.</h2>
        <p class="body" style="margin:18px 0 0;">Community is not a programme here; it is the way the base works. You will live closer to more people than you are used to, and most people say it is the part they miss most when they leave.</p>
      </div>
    </div>
    <div class="wrap cl-sticky">
      <ol class="cl-items">
        {COMMUNITY.map((c, i) => (
          <li class={`cl-item io-in${i === 0 ? ' is-on' : ''}`} data-i={i}>
            <p class="disp cl-item-n">{'0' + (i + 1)}</p>
            <h3 class="disp cl-item-title">{c.title}</h3>
            <span class="rule cl-item-rule"></span>
            <p class="body cl-item-body">{c.body}</p>
            <figure class="cl-item-photo">
              {c.photo ? <img src={c.photo} alt={c.alt ?? ''} loading="lazy" decoding="async" style={c.pos ? `object-position:${c.pos}` : undefined} /> : <PhotoSlot label={c.label} radius={0} />}
            </figure>
          </li>
        ))}
      </ol>
      <div class="cl-stage" aria-hidden="true">
        <div class="cl-stage-inner">
          {COMMUNITY.map((c, i) => (
            <figure class={`cl-stage-photo${i === 0 ? ' is-on' : ''}`} data-i={i}>
              {c.photo ? <img src={c.photo} alt="" loading="lazy" decoding="async" style={c.pos ? `object-position:${c.pos}` : undefined} /> : <PhotoSlot label={c.label} radius={0} />}
            </figure>
          ))}
        </div>
      </div>
    </div>
  </section>

  <section class="cl-stats" aria-label="Campus in numbers">
    <div class="wrap cl-stats-grid">
      {STATS.map((s) => (
        <div class="cl-stat io-in">
          <p class="disp cl-stat-n"><span data-count={s.n}>0</span>{s.suffix}</p>
          <p class="muted cl-stat-l">{s.label}</p>
        </div>
      ))}
    </div>
  </section>

  <section class="sec cl-sched-sec" id="schedule">
    <div class="wrap cl-hs-head">
      <div class="reveal">
        <p class="eyebrow">Base schedule</p>
        <h2 class="disp" style="margin:12px 0 0;font-size:clamp(28px,3.4vw,46px);font-weight:500;line-height:1.08;">The shape of a week.</h2>
        <p class="body" style="margin:14px 0 0;max-width:52ch;">Every weekday starts with breakfast and chores together. What comes next at 8:30 depends on the day.</p>
      </div>
      <div class="cl-hs-nav" aria-hidden="true">
        <button type="button" class="cl-hs-btn" data-hs-prev="sched">←</button>
        <button type="button" class="cl-hs-btn" data-hs-next="sched">→</button>
      </div>
    </div>
    <div class="cl-hs" data-hs="sched">
      <p class="disp cl-hs-label" aria-hidden="true">Base<br />schedule</p>
      <ol class="cl-hs-track sched-track">
        {WEEK.map((d) => (
          <li class="sched-day io-in">
            <p class="disp sched-day-n">{d.day}</p>
            <p class="muted sched-day-f">{d.full}</p>
            <ul class="sched-slots">
              {d.slots.map((sl) => (
                <li class={`sched-slot${sl.hi ? ' is-hi' : ''}`}>
                  <span class="sched-time">{sl.time}</span>
                  <b class="sched-title">{sl.title}</b>
                  {sl.note && <span class="sched-note">{sl.note}</span>}
                </li>
              ))}
            </ul>
          </li>
        ))}
      </ol>
    </div>
    <div class="wrap"><p class="muted" style="margin:22px 0 0;font-size:13px;">Times are the base rhythm; schools add their own lectures, work duties and outreach prep around it.</p></div>
  </section>

  <section class="sec cl-fac-sec" id="campus" style="padding-top:0;">
    <div class="wrap cl-hs-head">
      <div class="reveal">
        <p class="eyebrow">Around campus</p>
        <h2 class="disp" style="margin:12px 0 0;font-size:clamp(28px,3.4vw,46px);font-weight:500;line-height:1.08;">Where the days happen.</h2>
      </div>
      <div class="cl-hs-nav" aria-hidden="true">
        <button type="button" class="cl-hs-btn" data-hs-prev="fac">←</button>
        <button type="button" class="cl-hs-btn" data-hs-next="fac">→</button>
      </div>
    </div>
    <div class="cl-hs" data-hs="fac">
      <p class="disp cl-hs-label" aria-hidden="true">Campus<br />facilities</p>
      <ul class="cl-hs-track fac-track">
        {FACILITIES.map((f) => (
          <li class="fac-card io-in">
            <figure class="fac-photo">
              {f.photo ? <img src={f.photo} alt={f.alt ?? ''} loading="lazy" decoding="async" style={f.pos ? `object-position:${f.pos}` : undefined} /> : <PhotoSlot label={f.label ?? ''} radius={0} />}
            </figure>
            <h3 class="disp fac-title">{f.title}</h3>
            <p class="body fac-note">{f.note}</p>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <section class="sec cl-food" id="food">
    <div class="wrap">
      <div class="cl-food-grid">
        <figure class="cl-food-photo wipe">
          <div class="parslow" style="height:116%;margin-top:-8%;">
            <img src="/images/campus/food-prep.jpg" alt="Khmer women in blue preparing sticky rice in bamboo and palm-leaf wrapped food at a market table" loading="lazy" decoding="async" style="object-position:50% 60%;" />
          </div>
        </figure>
        <div class="cl-food-copy linein">
          <p class="eyebrow">Food</p>
          <h2 class="disp" style="margin:14px 0 0;font-size:clamp(30px,3.8vw,50px);font-weight:500;line-height:1.08;">Khmer culture happens around food.</h2>
          <p class="body" style="margin:18px 0 0;max-width:50ch;">Nobody here eats alone. The first question you will be asked every day is whether you have eaten yet — and it is a real question. Meals are shared, rice is the centre of every one of them, and fruit appears whenever people gather.</p>
          <p class="body" style="margin:14px 0 0;max-width:50ch;">Expect fish amok, sour soups, grilled everything, greens you cannot name yet, and a lot of chilli you are allowed to leave on the side.</p>
        </div>
      </div>

      <div class="card cl-cafe reveal io-in">
        <figure class="cl-cafe-photo">
          <img src="/images/campus/gp-cafe-sign.jpg" alt="The YWAM sign and the GP Cafe sign hanging among green leaves, with an iced green drink held up in front" loading="lazy" decoding="async" />
        </figure>
        <div class="cl-cafe-copy">
          <p class="eyebrow">On campus</p>
          <h3 class="disp" style="margin:12px 0 0;font-size:clamp(26px,3vw,40px);font-weight:500;line-height:1.1;">GP Cafe</h3>
          <p class="body" style="margin:16px 0 0;max-width:46ch;">Our own café, right on campus. Khmer coffee, iced drinks and something sweet, made by our community for our community and the neighbourhood around us. It is where conversations that start in class carry on.</p>
          <p class="muted" style="margin:14px 0 0;font-size:14px;">Open to students, staff, teams and neighbours.</p>
        </div>
        <figure class="cl-cafe-barista">
          <img src="/images/campus/gp-cafe-barista.jpg" alt="A smiling barista in an apron behind the GP Cafe counter, flashing a peace sign" loading="lazy" decoding="async" />
        </figure>
      </div>
    </div>
  </section>

  <section class="sec" style="padding-top:0;">
    <div class="wrap">
      <p class="eyebrow reveal">Who lives here</p>
      <div class="cl-who stag" style="margin-top:22px;">
        {WHO.map(([t, d]) => (
          <div class="card cl-who-card">
            <h3 class="disp" style="margin:0;font-size:20px;font-weight:500;">{t}</h3>
            <p class="body" style="margin:12px 0 0;font-size:15px;">{d}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section class="city-stack" id="siem-reap" aria-label="Things to do in Siem Reap">
    <div class="city-pin">
      <div class="wrap city-head">
        <div>
          <p class="eyebrow">Siem Reap</p>
          <h2 class="disp" style="margin:12px 0 0;font-size:clamp(28px,3.4vw,46px);font-weight:500;line-height:1.08;">The city is part of the training.</h2>
        </div>
        <div class="city-progress" aria-hidden="true">
          {[...CITY, { title: 'Good to know' }].map((p, i) => <span class="city-dot" data-i={i} title={p.title}></span>)}
          <span class="muted city-cue">Scroll →</span>
        </div>
      </div>
      <div class="city-track">
        {CITY.map((g, i) => (
          <article class="city-panel io-in" data-i={i}>
            <figure class="city-photo">{g.photo ? <img src={g.photo} alt={g.alt ?? ''} loading="lazy" decoding="async" style={`display:block;width:100%;height:100%;object-fit:cover;object-position:${g.pos ?? '50% 50%'}`} /> : <PhotoSlot label={g.label} radius={0} />}</figure>
            <div class="city-copy">
              <p class="disp" style="margin:0;font-size:13px;letter-spacing:.14em;color:var(--sr-pink);">{'0' + (i + 1)} · {CITY.length + 1}</p>
              <h3 class="disp" style="margin:10px 0 0;font-size:clamp(24px,2.4vw,32px);font-weight:500;line-height:1.12;">{g.title}</h3>
              <p class="body" style="margin:12px 0 0;font-size:15px;">{g.intro}</p>
              <ul class="city-list">
                {g.places.map((p) => <li><b>{p.name}</b><span>{p.note}</span></li>)}
              </ul>
            </div>
          </article>
        ))}
        <article class="city-panel city-panel--practical io-in" data-i={CITY.length}>
          <figure class="city-photo"><img src="/images/campus/tuktuk.jpg" alt="Riding through Siem Reap in the back of a tuk-tuk, the driver in a white helmet ahead on a tree-lined road" loading="lazy" decoding="async" style="display:block;width:100%;height:100%;object-fit:cover;" /></figure>
          <div class="city-copy">
            <p class="disp" style="margin:0;font-size:13px;letter-spacing:.14em;color:var(--sr-pink);">{'0' + (CITY.length + 1)} · {CITY.length + 1}</p>
            <h3 class="disp" style="margin:10px 0 0;font-size:clamp(24px,2.4vw,32px);font-weight:500;line-height:1.12;">Good to know</h3>
            <dl class="practical">
              {PRACTICAL.map(([k, v]) => (
                <div>
                  <dt class="disp" style="font-size:15px;font-weight:500;">{k}</dt>
                  <dd class="body" style="margin:6px 0 0;font-size:14px;">{v}</dd>
                </div>
              ))}
            </dl>
          </div>
        </article>
      </div>
    </div>
  </section>

  <Cta
    title="Come and live it."
    body="Whether you are coming for a school, to volunteer, to join staff or with a team, this is the community you are walking into."
    primary={['apply', 'Apply']}
    secondary={['visit', 'Bring a team']}
  />
  <Footer />
</SrLayout>

<script>
  // Counters tick up when the stats strip comes into view; the Siem Reap
  // track slides horizontally with scroll while its section is pinned
  // (desktop only — on phones and reduced-motion the panels stack).
  let ctrl = null;
  const init = () => {
    if (ctrl) ctrl.abort();
    ctrl = new AbortController();
    const { signal } = ctrl;
    const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    const counters = [...document.querySelectorAll('[data-count]')];
    if (counters.length) {
      const run = (el) => {
        const target = Number(el.dataset.count);
        if (reduce) { el.textContent = String(target); return; }
        const t0 = performance.now();
        const dur = 1200;
        const tick = (now) => {
          const t = Math.min(1, (now - t0) / dur);
          const e = 1 - Math.pow(1 - t, 3);
          el.textContent = String(Math.round(target * e));
          if (t < 1) requestAnimationFrame(tick);
        };
        requestAnimationFrame(tick);
      };
      const io = new IntersectionObserver((entries) => {
        entries.forEach((en) => { if (en.isIntersecting) { run(en.target); io.unobserve(en.target); } });
      }, { threshold: 0.6 });
      counters.forEach((c) => io.observe(c));
      signal.addEventListener('abort', () => io.disconnect());
    }

    const items = [...document.querySelectorAll('.cl-item')];
    const stagePhotos = [...document.querySelectorAll('.cl-stage-photo')];
    const stageKm = document.querySelector('[data-stage-km]');
    const kms = ["គ្រួសារ", "ថ្វាយបង្គំ", "បម្រើ"];
    if (items.length && stagePhotos.length) {
      let cur = -1;
      const pickItem = () => {
        const mid = window.innerHeight * 0.5;
        let n = 0;
        let best = Infinity;
        items.forEach((it, i) => {
          const r = it.getBoundingClientRect();
          const d = Math.abs((r.top + Math.min(r.height, 320) / 2) - mid);
          if (d < best) { best = d; n = i; }
        });
        if (n === cur) return;
        cur = n;
        items.forEach((it, i) => it.classList.toggle('is-on', i === n));
        stagePhotos.forEach((ph, i) => ph.classList.toggle('is-on', i === n));
        if (stageKm) stageKm.textContent = kms[n] || '';
      };
      let raf2 = 0;
      const onScroll2 = () => { if (!raf2) raf2 = requestAnimationFrame(() => { raf2 = 0; pickItem(); }); };
      window.addEventListener('scroll', onScroll2, { passive: true, signal });
      window.addEventListener('resize', onScroll2, { signal });
      pickItem();
    }

    // Arrow buttons for the horizontal strips (schedule, facilities).
    document.querySelectorAll('[data-hs]').forEach((hs) => {
      const key = hs.dataset.hs;
      const track = hs.querySelector('.cl-hs-track');
      const step = () => { const first = track.firstElementChild; return first ? first.getBoundingClientRect().width + 18 : 320; };
      document.querySelectorAll(`[data-hs-prev="${key}"]`).forEach((b) => b.addEventListener('click', () => track.scrollBy({ left: -step(), behavior: 'smooth' }), { signal }));
      document.querySelectorAll(`[data-hs-next="${key}"]`).forEach((b) => b.addEventListener('click', () => track.scrollBy({ left: step(), behavior: 'smooth' }), { signal }));
    });

    const stack = document.querySelector('.city-stack');
    const track = document.querySelector('.city-track');
    const dots = [...document.querySelectorAll('.city-dot')];
    const panels = [...document.querySelectorAll('.city-panel')];
    if (!stack || !track || !panels.length) return;
    const pinned = () => window.matchMedia('(min-width: 1001px) and (prefers-reduced-motion: no-preference)').matches;
    // Phones: the track is a swipeable snap carousel; keep the dots in step.
    const onTrackScroll = () => {
      if (pinned()) return;
      const w = panels[0]?.offsetWidth || 1;
      const gap = 14;
      const idx = Math.min(panels.length - 1, Math.round(track.scrollLeft / (w + gap)));
      dots.forEach((d, i) => d.classList.toggle('is-on', i <= idx));
      panels.forEach((pn, i) => pn.classList.toggle('is-on', i === idx));
    };
    track.addEventListener('scroll', onTrackScroll, { passive: true, signal });
    const update = () => {
      if (!pinned()) {
        track.style.transform = '';
        onTrackScroll();
        return;
      }
      const rect = stack.getBoundingClientRect();
      const cpin = stack.querySelector('.city-pin');
      const scrollable = rect.height - (cpin ? cpin.offsetHeight : window.innerHeight);
      const p = scrollable <= 0 ? 0 : Math.min(1, Math.max(0, -rect.top / scrollable));
      const max = track.scrollWidth - track.parentElement.clientWidth;
      const eased = Math.min(1, Math.max(0, (p - 0.03) / 0.94));
      track.style.transform = `translateX(${(-eased * max).toFixed(1)}px)`;
      const idx = Math.min(panels.length - 1, Math.round(eased * (panels.length - 1)));
      dots.forEach((d, i) => d.classList.toggle('is-on', i <= idx));
      panels.forEach((pn, i) => pn.classList.toggle('is-on', i === idx));
    };
    let raf = 0;
    const onScroll = () => { if (!raf) raf = requestAnimationFrame(() => { raf = 0; update(); }); };
    window.addEventListener('scroll', onScroll, { passive: true, signal });
    window.addEventListener('resize', onScroll, { signal });
    update();
  };
  init();
  document.addEventListener('astro:page-load', init);
</script>
